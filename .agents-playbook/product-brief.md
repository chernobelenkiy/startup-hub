# Startup Hub — PRD

## 🎯 Концепция

Мультиязычный агрегатор стартап-проектов на ранних стадиях с AI-first подходом.

---

## 💎 УТП: Чем отличаемся от Product Hunt / BetaList

| Product Hunt, BetaList | Startup Hub |
|------------------------|-------------|
| Готовые продукты, запуски | Проекты на любой стадии — от идеи до запуска |
| Фокус на продукте | Фокус на команде и людях |
| Только витрина | Поиск команды + инвестиций + единомышленников |
| Высокий порог входа | Низкий порог — можно прийти с идеей |
| Все индустрии | Фокус на AI/tech проектах |
| Нет API для автоматизации | MCP интеграция для AI-агентов |

### Ключевая идея

Startup Hub — это не "посмотрите мой готовый продукт", а:
- "Ищу разработчика в команду"
- "Нужен ментор для AI-проекта"  
- "Готов вложить время в интересный стартап"
- "Есть идея, ищу co-founder"

### Целевая ниша

**AI/Tech проекты** — платформа изначально ориентирована на технологические стартапы и AI-проекты. Это позволяет:
- Привлечь релевантную аудиторию
- Не конкурировать напрямую с Product Hunt
- Использовать MCP как естественное преимущество

---

## 📊 Сущности (Data Model)

### User
```
id: UUID
email: string (unique)
name: string
avatar_url: string?
locale: 'en' | 'ru' | ...
created_at: timestamp
```

### Project
```
id: UUID
owner_id: UUID → User
slug: string (unique, URL-friendly)

# Основное
title: string
short_description: string (max 280 chars)
pitch: string (max 500 chars) — "почему и зачем"
screenshot_url: string?
website_url: string?

# Статусы
status: 'idea' | 'mvp' | 'beta' | 'launched' | 'paused'
estimated_launch: date?
needs_investment: boolean
investment_details: string?

# Команда
team_members: JSON [{name, role, avatar?}]
looking_for: string[] — ['developer', 'designer', 'marketer', ...]

# Мета
tags: string[]
likes_count: int (denormalized)
language: string — язык оригинала
created_at: timestamp
updated_at: timestamp
```

### Like
```
user_id: UUID → User
project_id: UUID → Project
created_at: timestamp
(unique: user_id + project_id)
```

### APIToken (для MCP)
```
id: UUID
user_id: UUID → User
token_hash: string
name: string — "My AI Agent"
permissions: ['create', 'update', 'delete']
last_used_at: timestamp?
created_at: timestamp
expires_at: timestamp?
```

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                      Frontend                            │
│  Next.js 14 (App Router) + Tailwind + shadcn/ui         │
│  ├── / (главная с проектами)                            │
│  ├── /auth (login/register)                             │
│  ├── /project/[slug] (детали проекта)                   │
│  ├── /dashboard (мои проекты + токены)                  │
│  └── Мультиязычность: next-intl                         │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    API Layer                             │
│  Next.js API Routes                                      │
│  ├── /api/auth/* (NextAuth)                             │
│  ├── /api/projects (CRUD)                               │
│  ├── /api/projects/[id]/like                            │
│  ├── /api/tokens (управление API токенами)              │
│  └── /api/mcp/* (для AI агентов)                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                    Database                              │
│  PostgreSQL (Supabase / Neon / Railway)                 │
│  + Prisma ORM                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🌍 Мультиязычность

### Автоопределение языка:
1. **Cookie** `locale` (если уже выбирал)
2. **Accept-Language** header
3. **IP Geolocation** (fallback) — через Vercel Edge Middleware
4. **Default:** `en`

### Реализация:
```typescript
// middleware.ts
import { NextRequest } from 'next/server'
import createMiddleware from 'next-intl/middleware'

export default createMiddleware({
  locales: ['en', 'ru'],
  defaultLocale: 'en',
  localeDetection: true
})
```

### Контент проектов:
- Проекты создаются на языке автора
- Поле `language` указывает оригинальный язык
- (v2) Опциональные переводы через JSON поле `translations`

---

## 🔍 Поиск и Фильтрация

### Фильтры:
- **Статус:** idea, mvp, beta, launched, paused
- **Ищут в команду:** developer, designer, marketer, etc.
- **Инвестиции:** needs/doesn't need
- **Теги:** технологии, индустрии

### Сортировка:
- По дате (новые/старые)
- По лайкам
- По дате запуска

### Поиск:
- Full-text search по title + description + pitch
- PostgreSQL `tsvector` или Meilisearch для v2

---

## 🤖 MCP API

### Эндпоинты:
```
POST   /api/mcp/projects     — создать проект
GET    /api/mcp/projects     — список моих проектов  
GET    /api/mcp/projects/:id — детали проекта
PUT    /api/mcp/projects/:id — обновить проект
DELETE /api/mcp/projects/:id — удалить проект
```

### Аутентификация:
```
Authorization: Bearer <api_token>
```

### MCP Server (для Claude, GPT и др.):
```typescript
// Инструменты MCP
tools: [
  {
    name: 'create_project',
    description: 'Create a new startup project',
    inputSchema: { ... }
  },
  {
    name: 'update_project',
    description: 'Update existing project',
    inputSchema: { ... }
  },
  {
    name: 'delete_project', 
    description: 'Delete a project',
    inputSchema: { projectId: string }
  },
  {
    name: 'list_my_projects',
    description: 'List all my projects'
  }
]
```

---

## 🎨 UI/UX (Одностраничник)

```
┌────────────────────────────────────────────────────────────┐
│  🌐 StartupHub                    [EN ▾] [Login] [Sign up] │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  🔍 Search projects...                                     │
│                                                            │
│  [Status ▾] [Looking for ▾] [Investment ▾] [Tags ▾]       │
│  Sort by: [Newest ▾]                                       │
│                                                            │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │ 📸 Screenshot    │  │ 📸 Screenshot    │               │
│  │                  │  │                  │               │
│  │ ProjectName      │  │ ProjectName      │               │
│  │ Short desc...    │  │ Short desc...    │               │
│  │                  │  │                  │               │
│  │ 🏷️ tag1 tag2     │  │ 🏷️ tag1 tag2     │               │
│  │ 👥 3 in team     │  │ 👥 2 in team     │               │
│  │ 🔍 Need: dev     │  │ 💰 Seeking inv.  │               │
│  │                  │  │                  │               │
│  │ [MVP] ❤️ 42      │  │ [IDEA] ❤️ 18    │               │
│  └──────────────────┘  └──────────────────┘               │
│                                                            │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │ ...              │  │ ...              │               │
│  └──────────────────┘  └──────────────────┘               │
│                                                            │
│                    [Load more]                             │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
startup-hub/
├── app/
│   ├── [locale]/
│   │   ├── page.tsx              # Главная
│   │   ├── layout.tsx
│   │   ├── auth/
│   │   │   ├── login/page.tsx
│   │   │   └── register/page.tsx
│   │   ├── project/
│   │   │   └── [slug]/page.tsx   # Детали проекта
│   │   └── dashboard/
│   │       ├── page.tsx          # Мои проекты
│   │       └── tokens/page.tsx   # API токены
│   └── api/
│       ├── auth/[...nextauth]/
│       ├── projects/
│       ├── tokens/
│       └── mcp/
├── components/
│   ├── ui/                       # shadcn
│   ├── ProjectCard.tsx
│   ├── ProjectFilters.tsx
│   ├── ProjectForm.tsx
│   └── ...
├── lib/
│   ├── db.ts                     # Prisma client
│   ├── auth.ts                   # NextAuth config
│   └── i18n.ts
├── messages/
│   ├── en.json
│   └── ru.json
├── prisma/
│   └── schema.prisma
├── mcp-server/                   # Отдельный MCP сервер
│   ├── index.ts
│   └── tools.ts
└── ...
```

---

## 🚀 MVP Scope (v1)

### Must Have:
- [ ] Регистрация / Логин (email + OAuth Google)
- [ ] Создание/редактирование проекта
- [ ] Список проектов с карточками
- [ ] Базовые фильтры (статус, теги)
- [ ] Лайки
- [ ] Мультиязычность (EN/RU)
- [ ] API токены
- [ ] MCP эндпоинты

### Nice to Have (v2):
- [ ] Комментарии
- [ ] Фолловинг проектов
- [ ] Уведомления
- [ ] Полнотекстовый поиск (Meilisearch)
- [ ] Переводы контента проектов
- [ ] Верификация проектов
- [ ] Аналитика просмотров

---

## ⏱️ Оценка времени

| Этап | Часы |
|------|------|
| Настройка проекта + БД | 4 |
| Auth (NextAuth) | 4 |
| CRUD проектов | 8 |
| UI главной + карточки | 8 |
| Фильтры и поиск | 6 |
| Мультиязычность | 4 |
| API токены | 4 |
| MCP Server | 6 |
| Тестирование + деплой | 6 |
| **Итого** | **~50 часов** |

---

## 🛠️ Стек

| Категория | Технология |
|-----------|------------|
| Framework | Next.js 14 (App Router) |
| Styling | Tailwind CSS + shadcn/ui |
| Database | PostgreSQL (Supabase) |
| ORM | Prisma |
| Auth | NextAuth.js |
| i18n | next-intl |
| Hosting | Vercel |
| MCP | @modelcontextprotocol/sdk |

---

## 💡 Заметки

1. **Теги как замена полей:** Можно вынести `looking_for` и `needs_investment` в теги с префиксами: `role:developer`, `investment:seeking`

2. **Slug генерация:** `slugify(title) + '-' + nanoid(6)` для уникальности

3. **Rate limiting:** Для MCP API важно добавить rate limits (upstash ratelimit)

4. **Изображения:** Vercel Blob или Cloudflare R2 для скриншотов